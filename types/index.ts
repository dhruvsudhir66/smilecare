export type Service = {
  slug: string;
  name: string;
  shortDescription: string;
  description: string;
  icon: string;
  bullets: string[];
};

export type Doctor = {
  name: string;
  role: string;
  specialty: string;
  bio: string;
};